schema: spec-driven

context: |
  Tech stack: C++, Arduino framework, PlatformIO
  Hardware: AI-Thinker ESP32-CAM-MB (OV2640 camera, SD slot, CH340 USB)
  Domain: embedded firmware for autonomous camera timelapse
  Language: All repository content (code, comments, docs, artifacts) MUST be written in English.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
